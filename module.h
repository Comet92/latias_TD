#pragma once

/* On affiches les éléments d'un tableau  */
void print_tableau( int t[], int n );

/* On calcul la somme de deux tableaux de même tailles dans un troisième tableau t  */
void sumTab(int t1[], int t2[],int t[], int n);
